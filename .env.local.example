# 🔒 Local Environment Variables (NOT committed to git)
#
# Copy this file to .env.local and add configuration:
#   cp .env.local.example .env.local
#
# Online Search Configuration:
# The application uses SearXNG + Crawl4AI by default (NO API KEY REQUIRED!)
# Simply run: ./setup_searxng.sh to start the local search engine

# ====== OPTION 1: SearXNG + Crawl4AI (DEFAULT - Free, Open Source, No API key!) ======
# This is already configured and ready to use!
# Just run: ./setup_searxng.sh
ONLINE_SEARCH_PROVIDER=searxng

# Optional SearXNG configuration:
# SEARXNG_INSTANCES=http://localhost:8080  # Your local instance

# === IP Ban Prevention Settings ===
# Adjust these to control search frequency and avoid rate limiting
# Lower values = safer but slower | Higher values = faster but riskier

# Rate Limiting (Token Bucket Algorithm)
# SEARXNG_RATE_LIMIT=2.0                   # Requests per second (default: 2.0)
# SEARXNG_BURST_LIMIT=5.0                  # Burst capacity tokens (default: 5.0)
# SEARXNG_MIN_DELAY=1.0                    # Min seconds between requests (default: 1.0)

# Retry & Backoff (For Transient Errors)
# SEARXNG_MAX_RETRIES=3                    # Max retries on failure (default: 3)
# SEARXNG_BACKOFF=2.0                      # Initial backoff delay seconds (default: 2.0)

# Caching (Reduces Duplicate Requests - HIGHLY RECOMMENDED)
# SEARXNG_CACHE=1                          # 1=enabled, 0=disabled (default: 1)
# SEARXNG_CACHE_TTL=604800                 # Cache TTL in seconds - 7 days (default)
# SEARXNG_CACHE_DB_PATH=data/duckdb/searxng_cache.db

# Other Settings
# SEARXNG_TIMEOUT=30                       # Request timeout seconds (default: 30)
# SEARXNG_CRAWL=0                          # Set to 1 for full page crawling
# SEARXNG_LANGUAGE=en                      # Search language (en, pt-BR, es, etc)

# === Recommended Presets ===
# MAXIMUM SAFETY (Slow but very safe):
#   SEARXNG_RATE_LIMIT=1.0, SEARXNG_MIN_DELAY=2.0, SEARXNG_BURST_LIMIT=2.0
#
# BALANCED (Default - Good for most use cases):
#   SEARXNG_RATE_LIMIT=2.0, SEARXNG_MIN_DELAY=1.0, SEARXNG_BURST_LIMIT=5.0
#
# AGGRESSIVE (Fast but higher risk):
#   SEARXNG_RATE_LIMIT=5.0, SEARXNG_MIN_DELAY=0.5, SEARXNG_BURST_LIMIT=10.0
#
# See IP_BAN_PREVENTION.md for detailed guidance

# ====== OPTION 2: xAI Grok (Alternative if you have API key) ======
# Get API key from: https://console.x.ai
# ONLINE_SEARCH_PROVIDER=grok
# GROK_API_KEY=your_grok_api_key_here
# Optional overrides:
# GROK_MODEL=grok-beta
# GROK_BASE_URL=https://api.x.ai/v1
# GROK_TIMEOUT=60

# ====== OPTION 3: Google Gemini (Alternative if you have API key) ======
# Get API key from: https://aistudio.google.com/app/apikey
# ONLINE_SEARCH_PROVIDER=gemini
# GOOGLE_API_KEY=your_gemini_api_key_here
# Optional overrides:
# GEMINI_MODEL=gemini-2.0-flash
# GEMINI_BASE_URL=https://generativelanguage.googleapis.com
# GEMINI_TIMEOUT=60
