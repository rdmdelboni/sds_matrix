# ⚡ Configuração de Performance Otimizada - FDS Extractor
#
# Este arquivo contém configurações otimizadas para diferentes cenários.
# Copie as configurações desejadas para seu arquivo .env

# ==============================================================================
# 🚀 CONFIGURAÇÃO 1: MÁXIMA VELOCIDADE (Triagem Rápida)
# ==============================================================================
# Use para: Processar grandes volumes rapidamente com qualidade razoável
# Ideal para: Primeira triagem de 500+ arquivos
# Tempo estimado para 500 arquivos: 15-25 minutos

# MAX_WORKERS=8                          # Máximo paralelismo
# CHUNK_SIZE=2000                        # Chunks menores = mais rápido
# LM_STUDIO_MODEL=phi3:mini              # Modelo pequeno e rápido (~2GB)
# LM_STUDIO_TEMPERATURE=0.0              # Mais determinístico
# LM_STUDIO_MAX_TOKENS=1000              # Respostas mais curtas
# ONLINE_SEARCH_PROVIDER=gemini          # Gemini é mais rápido que LLM local
# GOOGLE_API_KEY=sua_chave_aqui          # Obtenha em https://makersuite.google.com/app/apikey

# ==============================================================================
# ⚖️ CONFIGURAÇÃO 2: BALANCEADA (Velocidade + Qualidade)
# ==============================================================================
# Use para: Equilíbrio entre velocidade e precisão
# Ideal para: Processamento geral, volumes médios (50-300 arquivos)
# Tempo estimado para 500 arquivos: 25-40 minutos

MAX_WORKERS=4                            # Bom paralelismo
CHUNK_SIZE=4000                          # Chunks balanceados
LM_STUDIO_MODEL=llama3.2:3b              # Modelo médio (~2GB)
LM_STUDIO_TEMPERATURE=0.1                # Levemente criativo
LM_STUDIO_MAX_TOKENS=1500                # Respostas moderadas
ONLINE_SEARCH_PROVIDER=gemini            # Use Gemini se disponível
# GOOGLE_API_KEY=sua_chave_aqui

# ==============================================================================
# 🎯 CONFIGURAÇÃO 3: MÁXIMA QUALIDADE (Precisão Máxima)
# ==============================================================================
# Use para: Resultados finais com máxima precisão
# Ideal para: Volumes pequenos (< 50 arquivos) ou dados críticos
# Tempo estimado para 500 arquivos: 50-90 minutos

# MAX_WORKERS=2                          # Processamento conservador
# CHUNK_SIZE=6000                        # Chunks maiores = mais contexto
# LM_STUDIO_MODEL=llama3.1:8b            # Modelo grande (~4.7GB)
# LM_STUDIO_TEMPERATURE=0.1              # Preciso
# LM_STUDIO_MAX_TOKENS=2000              # Respostas detalhadas
# ONLINE_SEARCH_PROVIDER=lmstudio        # LLM local para controle total

# ==============================================================================
# 💻 CONFIGURAÇÃO 4: HARDWARE LIMITADO (RAM < 8GB)
# ==============================================================================
# Use para: Computadores com pouca RAM ou CPU fraca
# Ideal para: Hardware limitado
# Tempo estimado para 500 arquivos: 80-120 minutos

# MAX_WORKERS=1                          # Sem paralelismo
# CHUNK_SIZE=3000                        # Chunks médios
# LM_STUDIO_MODEL=phi3:mini              # Modelo leve
# LM_STUDIO_TEMPERATURE=0.0
# LM_STUDIO_MAX_TOKENS=1000
# ONLINE_SEARCH_PROVIDER=gemini          # Recomendado para economizar recursos locais

# ==============================================================================
# ☁️ CONFIGURAÇÃO 5: SOMENTE NUVEM (Sem LLM Local)
# ==============================================================================
# Use para: Quando não tem Ollama instalado ou quer máxima velocidade
# Ideal para: Processamento na nuvem, sem instalar modelos locais
# Tempo estimado para 500 arquivos: 10-20 minutos
# Custo estimado: $0.10-0.30 para 500 arquivos

# MAX_WORKERS=6                          # Alto paralelismo (Gemini aguenta)
# CHUNK_SIZE=4000
# ONLINE_SEARCH_PROVIDER=gemini
# GOOGLE_API_KEY=sua_chave_aqui          # OBRIGATÓRIO
# GEMINI_MODEL=gemini-2.0-flash          # Modelo rápido
# GEMINI_TIMEOUT=60

# ==============================================================================
# 🔧 OUTRAS CONFIGURAÇÕES
# ==============================================================================

# Diretório de dados (opcional - padrão: ./data)
# FDS_DATA_DIR=/caminho/customizado/data

# Configuração do Ollama/LM Studio
LM_STUDIO_BASE_URL=http://127.0.0.1:11434/v1    # Endpoint Ollama
# LM_STUDIO_BASE_URL=http://127.0.0.1:1234/v1   # Endpoint LM Studio
LM_STUDIO_TIMEOUT=60

# Limites de arquivo
MAX_FILE_SIZE_MB=10                      # Máximo 10MB por arquivo
CHUNK_SIZE=4000                          # Tamanho dos chunks de texto

# Logging
LOG_LEVEL=INFO                           # DEBUG | INFO | WARNING | ERROR
# LOG_FILE=/caminho/customizado/app.log

# Tesseract OCR (para PDFs escaneados)
# TESSERACT_PATH=/usr/bin/tesseract     # Linux
# TESSERACT_PATH=C:\Program Files\Tesseract-OCR\tesseract.exe  # Windows

# ==============================================================================
# 📊 COMO ESCOLHER A MELHOR CONFIGURAÇÃO
# ==============================================================================
#
# 1. Execute o benchmark:
#    python benchmark_performance.py /sua/pasta/fds --compare --files 10
#
# 2. Veja qual configuração tem melhor throughput (arquivos/segundo)
#
# 3. Ajuste MAX_WORKERS baseado em:
#    - CPU cores: use 1-2 workers por core físico
#    - RAM: ~500MB-1GB por worker (com LLM local)
#    - VRAM: ~2-8GB dependendo do modelo LLM
#
# 4. Teste incrementalmente:
#    - Comece com 10 arquivos
#    - Depois 50 arquivos
#    - Depois volume completo
#
# ==============================================================================
# 💡 DICAS DE PERFORMANCE
# ==============================================================================
#
# ✅ Use Gemini para grandes volumes (muito mais rápido que LLM local)
# ✅ Use modelos menores (phi3, llama3.2:3b) para triagem
# ✅ Use modelos maiores (llama3.1:8b) apenas para dados críticos
# ✅ Aumente MAX_WORKERS até CPU/RAM saturar
# ✅ Use SSD em vez de HDD (3-5x mais rápido)
# ✅ Monitore recursos: htop (Linux) ou Task Manager (Windows)
# ✅ Processe arquivos pequenos primeiro (ordenar por tamanho)
#
# ❌ Não use workers > núcleos de CPU disponíveis
# ❌ Não use modelos grandes sem RAM/VRAM suficiente
# ❌ Não processe 500 arquivos de uma vez sem testar antes com 10-50
#
# ==============================================================================
# 🔗 RECURSOS ÚTEIS
# ==============================================================================
#
# - Instalar Ollama: https://ollama.ai
# - Modelos Ollama: https://ollama.ai/library
# - Gemini API Key: https://makersuite.google.com/app/apikey
# - Documentação: Ver OTIMIZACAO_PERFORMANCE.md
# - Benchmark: python benchmark_performance.py --help
#
# ==============================================================================
